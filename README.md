# Personal Portfolio Website

SEO-friendly, minimal single-page portfolio website built with Astro and deployable to GitHub Pages.

## Features

- 🚀 **Fast & Lightweight** - Built with Astro for optimal performance
- 📝 **Markdown-based Content** - Easy content management via Markdown files
- 🔍 **SEO Optimized** - Meta tags, structured data, sitemap, and robots.txt
- 📱 **Responsive Design** - Mobile-first, works on all devices
- 🎨 **Modern UI** - Clean, minimal design focused on content
- 🚢 **GitHub Pages Ready** - Automated deployment via GitHub Actions

## Project Structure

```
my-portfolio/
├── .devcontainer/          # Dev container configuration
├── .github/workflows/       # GitHub Actions workflows
├── public/                  # Static assets (robots.txt, favicon, etc.)
├── src/
│   ├── content/            # Markdown content files
│   │   ├── personal.md     # Personal information
│   │   ├── experiences/    # Work experience entries
│   │   ├── projects/       # Project entries
│   │   └── skills/          # Skills entries
│   ├── components/         # Astro components
│   ├── layouts/            # Page layouts
│   ├── pages/              # Pages (index.astro)
│   ├── styles/             # Global CSS
│   └── utils/              # Utility functions (SEO helpers)
└── astro.config.mjs         # Astro configuration
```

## Getting Started

### Prerequisites

- Node.js 20+ 
- npm or yarn

### Installation

1. Clone the repository:
```bash
git clone <your-repo-url>
cd my-portfolio
```

2. Install dependencies:
```bash
npm install
```

3. Start the development server:
```bash
npm run dev
```

The site will be available at `http://localhost:4321`

### Using Dev Container

If you're using VS Code with Dev Containers extension:

1. Open the project in VS Code
2. Press `F1` and select "Dev Containers: Reopen in Container"
3. Wait for the container to build and install dependencies
4. Run `npm run dev` in the terminal

The dev server will be accessible on port 4321.

## Content Management

All content is managed through Markdown files in the `src/content/` directory:

### Personal Information (`src/content/personal.md`)

Edit this file to update your personal information:

```markdown
---
name: "Your Name"
title: "Your Title"
bio: "Your bio here..."
location: "Your Location"
email: "your@email.com"
# ... other fields
---
```

### Experiences (`src/content/experiences/`)

Create a new `.md` file for each work experience:

```markdown
---
title: "Job Title"
company: "Company Name"
startDate: "2020-01"
endDate: "Present"
description: "Job description..."
technologies: ["React", "Node.js"]
---
```

### Projects (`src/content/projects/`)

Create a new `.md` file for each project:

```markdown
---
title: "Project Name"
description: "Project description..."
url: "https://project-url.com"
github: "https://github.com/user/repo"
technologies: ["React", "TypeScript"]
featured: true
---
```

### Skills (`src/content/skills/`)

Create a new `.md` file for each skill category:

```markdown
---
category: "Frontend"
items: ["React", "Vue.js", "TypeScript"]
---
```

## Building for Production

```bash
npm run build
```

The built site will be in the `dist/` directory.

## Deployment to GitHub Pages

### Setup

1. Update `astro.config.mjs`:
   - Set `site` to your GitHub Pages URL: `https://yourusername.github.io`
   - Set `base` to your repository name: `/your-repo-name`

2. Push your code to GitHub

3. Enable GitHub Pages:
   - Go to your repository Settings → Pages
   - Source: Select "GitHub Actions"

4. The GitHub Actions workflow will automatically deploy on every push to `main`

### Manual Deployment

If you prefer manual deployment:

1. Build the site: `npm run build`
2. Push the `dist` folder to the `gh-pages` branch

## SEO Configuration

The site includes:

- **Meta Tags**: Title, description, Open Graph, Twitter Cards
- **Structured Data**: JSON-LD schema for Person and Website
- **Sitemap**: Automatically generated by Astro
- **robots.txt**: Configured for search engines

Update SEO settings in:
- `src/utils/seo.ts` - SEO utility functions
- `src/layouts/Page.astro` - Page-level SEO
- `public/robots.txt` - Search engine directives

## Customization

### Styling

Edit `src/styles/global.css` to customize colors, fonts, and spacing. CSS variables are defined in `:root` for easy theming.

### Components

All components are in `src/components/`. Modify them to change the layout and structure.

## License

MIT

## Credits

Built with [Astro](https://astro.build/)
